#!/bin/bash

# Make this script executable
chmod +x "$0"

# Make other scripts executable
chmod +x test-blog-core.sh
chmod +x run-blog-server.sh
chmod +x kill-blog-servers.sh

echo "All scripts are now executable."
# Make this script executable
chmod +x "$0"

# Make other scripts executable
chmod +x run-blog-test.sh
chmod +x run-blog-server.sh
#!/bin/bash

echo "Making shell scripts executable..."
chmod +x *.sh
echo "Done!"
echo "All scripts are now executable."
